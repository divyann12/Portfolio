Types of styles:
inline(Least recommended) - written in html tag
external(highly recommended) - external css file created
internal
--------------------------------------------------------------------
How to select elements in css: 
General selectors
- element selector:
select tages using tag name
syn:
    tagname {
    property: value
}
2.class selector
 Can select class with (.)
3. to target specific id use:
(#)
heighest prioprity is of id selector, then class, then element.
REACT, NODE, JAVASCRIPT, BACKEND -> IMP topics
--------------------------------------------------------------------
Flexbox:-
align text
justify content
combinators
background Image
scimba
---------------------------------------------------------------------
7-7-2026
display: grid; 
grid template columns(width, width -- how many columns you need) of columns
for equal size use (fr) as width.
instead of writing it agian and again linke(1fr, 1fr ,1fr)
use- repeat(4, 1fr);
In grid to add height we can use grid template rows
We use grid tag when we need control over our columns because flexbox behaves in its own way.

Q) when do we use image and background image tag?